name: Backend CI/CD Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/ci.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies and build test
        working-directory: backend
        run: |
          npm install
          npm run build

      - name: Copy Files to EC2
        uses: appleboy/scp-action@v0.1.4
        with:
          host: <ELASTIC_IP>
          username: ubuntu
          key: ${{ secrets.UI_EC2_SSH_KEY }}
          source: "backend/."
          target: "~/deploy-api"

      - name: Build and Run with Docker Compose on EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: <ELASTIC_IP>
          username: ubuntu
          key: ${{ secrets.UI_EC2_SSH_KEY }}
          script: |
            # Install Docker if not installed
            if ! command -v docker &> /dev/null; then
              sudo apt-get update
              sudo apt-get install -y ca-certificates curl
              sudo install -m 0755 -d /etc/apt/keyrings
              sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
              sudo chmod a+r /etc/apt/keyrings/docker.asc
              echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
              sudo apt-get update
              sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
              sudo systemctl start docker
            fi
            cd ~/deploy-api
            sudo docker rm -f api-rest || true
            sudo docker build -t api-rest .
            sudo docker run -d --name api-rest -p 3000:3000 api-rest
            sudo docker image prune -f

      - name: Send Telegram Sticker
        if: success()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendSticker" \
            -d chat_id="${{ secrets.TELEGRAM_TO }}" \
            -d sticker="CAACAgIAAxkBAAERnjRqaKaLMjN7XRTnrWtHGxsFXZ4SKQACNDoAArR2CUgmpTx1tcQieD0E"

      - name: Send Telegram Notification (Success)
        uses: appleboy/telegram-action@master
        if: success()
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ Backend Deployment Successful!
            Application: api-rest
            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.event.head_commit.message }}
            Actor: ${{ github.actor }}

      - name: Send Telegram Sticker
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendSticker" \
            -d chat_id="${{ secrets.TELEGRAM_TO }}" \
            -d sticker="CAACAgUAAxkBAAERnjhqaKaq1MAUePutHWKxn2tJqkJcwwACnR0AAmJaIFfm9Eeebau1Vj0E"

      - name: Send Telegram Notification (Failure)
        uses: appleboy/telegram-action@master
        if: failure()
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ❌ Backend Deployment FAILED!
            Application: movie
            Repository: ${{ github.repository }}
            Branch: ${{ github.ref_name }}
            Commit: ${{ github.event.head_commit.message }}
            Actor: ${{ github.actor }}
            Please check the GitHub Actions logs immediately!